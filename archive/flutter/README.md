# Archived Flutter client

Legacy Flutter scaffold and docs from before the Expo React Native app in `mobile/`.

The active client is **`mobile/`** (Expo). Do not run Flutter commands from this folder unless you are intentionally recovering old code.

Contents moved here during repo cleanup (June 2026).
