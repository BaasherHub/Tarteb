/** Shared press / disabled opacity — use instead of magic numbers in Pressable styles */
export const interaction = {
  pressed: 0.85,
  cardPressed: 0.92,
  rowPressed: 0.75,
  disabled: 0.5,
  /** Subtle scale on press (buttons, cards) */
  pressScale: 0.97,
} as const;
