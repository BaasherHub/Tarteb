# App icon

`app_icon.png` — white briefcase on primary blue `#1A73E8`.

Regenerate:

```bash
dart run tool/generate_app_icon.dart
dart run flutter_launcher_icons
```

Replace `app_icon.png` with a custom 1024×1024 PNG to update store icons.
